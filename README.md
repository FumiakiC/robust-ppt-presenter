# robust-ppt-presenter
A resilient, zero-dependency PowerShell script for remote PowerPoint control via Web UI. Features a robust HttpListener implementation that prevents crashes from network disconnects. Ideal for presenters needing a portable, "no-install" solution on any Windows PC.
